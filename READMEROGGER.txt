recordar que los archivos:
installmpg.sh
mfs.c
mpg123.bin

deben de estar en la misma carpeta.

installmpg.sh son las librerias y el programita para poder reproducir las canciones.

una ves ejecutado el programa poner FILE -> ADD.DIR-> ok
luego ir a INSTALL LIB-< install mpg123.
